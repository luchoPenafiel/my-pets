import React, { ReactElement } from 'react';
import styled from 'styled-components';

const Wrapper = styled.div`
  height: 30px;
`;

const Separetor = (): ReactElement => {
  return <Wrapper />;
};

export default Separetor;
