import React, { ReactElement } from 'react';
import styled from 'styled-components';

const Wrapper = styled.div`
  height: 15px;
`;

const SmallSeparetor = (): ReactElement => {
  return <Wrapper />;
};

export default SmallSeparetor;
